
{% if user.roles.0 != "ROLE_ADMIN" %}
    <form method="post" action="{{ path('app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('etes-vous sur de vouloir supprimé le compte ?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
        <button class="btn btn-primary my-8 py-2 px-12 hover:italic hover:bg-cyan-300">Supprimer le compte</button>
    </form>
{% endif %}
