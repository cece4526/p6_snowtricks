{{ form_start(form) }}

{{ form_errors(form) }}

    {{ form_row(form.content) }}

    <button class="btn btn-primary my-8 py-2 px-12 hover:italic hover:bg-cyan-300">
        {{ button_label|default('envoyer le commentaire') }}
    </button>
{{ form_end(form) }}